<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->

        <!-- 应用的主要色调,actionBar默认使用该颜色，Toolbar导航栏的底色 -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <!-- 应用的主要暗色调,系统状态栏 默认使用该颜色 -->
        <item name="colorPrimaryDark">@color/black</item>
        <!-- 一般控件的选中效果默认采用该颜色,如 CheckBox,RadioButton,SwitchCompat，ProcessBar等-->
        <item name="colorAccent">@color/colorAccent</item>

        <!-- ActionBar -->
        <item name="android:textAllCaps">false</item>
        <item name="android:windowActionBar">false</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="TextStatusBar" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->

        <!-- 应用的主要色调,actionBar默认使用该颜色，Toolbar导航栏的底色 -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <!-- 应用的主要暗色调,系统状态栏 默认使用该颜色 -->
        <item name="colorPrimaryDark">@color/color_red</item>
        <!-- 一般控件的选中效果默认采用该颜色,如 CheckBox,RadioButton,SwitchCompat，ProcessBar等-->
        <item name="colorAccent">@color/colorAccent</item>
    </style>


    <!-- 公用的页面theme -->
    <style name="AppThemeNew" parent="@style/BaseAppTheme">
    </style>

    <style name="BaseAppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

        <!-- EditText colorControlActivated -->
        <item name="colorControlNormal">@color/color_edit_control_normal</item>
        <item name="colorControlHighlight">@color/color_edit_control_ctivated</item>
        <item name="colorControlActivated">@color/color_edit_control_ctivated</item>

        <!-- ActionBar -->
        <item name="android:textAllCaps">false</item>
        <item name="android:windowActionBar">false</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>

        <!-- Material Dialog Global Theming -->
        <item name="md_dark_theme">false</item>
        <item name="md_title_color">@color/color_text_title</item>
        <item name="md_content_color">@color/color_text_body</item>
        <item name="md_positive_color">@color/color_component_blue_positive</item>
        <item name="md_neutral_color">@color/color_text_label</item>
        <item name="md_negative_color">@color/color_text_label</item>
        <item name="md_widget_color">@color/color_text_yellow</item>
    </style>


    <!-- 继承可以用parent方式，同理，也可在名称中加入前缀，如Theme.AppCompat.Light.NoActionBar，此时不写parent也是可以的-->
<!--    <style name="Theme.AppCompat.Light.NoActionBar" parent="@style/Theme.AppCompat.Light">-->
<!--        <item name="android:windowNoTitle">true</item>-->
<!--    </style>-->
    <style name="AppTheme.Base" parent="Theme.AppCompat.Light.NoActionBar">
    </style>


    <!-- 提示对话框 -->
    <style name="dialog" parent="@android:style/Theme.Dialog">
        <!-- 边框 -->
        <item name="android:windowFrame">@null</item>
        <!-- 是否悬浮在Activity上 -->
        <item name="android:windowIsFloating">true</item>
        <!-- 半透明 -->
        <item name="android:windowIsTranslucent">false</item>
        <!-- 无标题 -->
        <item name="android:windowNoTitle">true</item>
        <!-- 模糊 -->
        <item name="android:backgroundDimEnabled">true</item>
        <!-- 背景透明 -->
        <item name="android:windowBackground">@android:color/transparent</item>
        <!-- 灰度 -->
        <item name="android:backgroundDimAmount">0.6</item>
        <!-- 对话框是否有遮盖 -->
        <item name="android:windowContentOverlay">@null</item>
        <!-- 背景缓存颜色 -->
        <item name="android:colorBackgroundCacheHint">@null</item>
        <!-- 内容颜色 -->
        <item name="colorAccent">@color/color_white</item>
    </style>

    <!-- toolbar_xxx.xml -->
    <style name="toolbar_common">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">50dp</item>
        <item name="android:background">@color/colorPrimary</item>
        <item name="android:gravity">center_vertical</item>
    </style>

    <style name="toolbar_main_activity_tv_title">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_gravity">center</item>
        <item name="android:textSize">@dimen/dimen_normal</item>
        <item name="android:textColor">@color/color_text_white</item>
    </style>

</resources>
